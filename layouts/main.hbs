<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="utf-8">
        <title>{{ title }}</title>
        <meta name="description" content="{{ description }}" />
        {{>maincss}}
    </head>
    <body style="height: 100vh">
        {{{ contents }}}
    </body>
</html>